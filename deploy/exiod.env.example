# Exio Server Configuration
# Copy this file to /etc/exio/exiod.env and modify as needed

# Server listening port (default: 8080)
EXIO_PORT=8080

# Authentication token (REQUIRED - generate a strong random token)
# Example: openssl rand -hex 32
EXIO_TOKEN=your-secret-token-here

# Base domain for tunnel subdomains
# Tunnels will be accessible at https://<subdomain>.<base-domain>
EXIO_BASE_DOMAIN=dev.example.com
